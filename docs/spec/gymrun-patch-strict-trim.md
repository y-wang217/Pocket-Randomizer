First, Patch: strict trim. The app fails to boot under GYMRUN_TRIM_STRICT=1 (openApp times out on .starter). Find what reads the trimmed learnsets or legality tables at startup: bisect by module import, not by guess. Likely candidates in order: core/capabilities.ts, the hmLearnsets generation, describeMove, tierInfo. Make the read lazy or move the data into a generated table under build-config as 4.6c's hmLearnsets.ts was meant to be. Do not weaken the trim. Strict trim green on the full suite, then commit. Report which module it was and what it was reading.
