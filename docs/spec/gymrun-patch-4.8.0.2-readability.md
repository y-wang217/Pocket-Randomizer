# GYMRUN patch 4.8.0.2 — readability

Committed verbatim before any work began on it, per protocol 7 in
[`README.md`](README.md). The brief as issued, unedited. The status report it
asks for first, the lift, and the tracker report live in
[`../visual/reports/patch-4.8.0.2.md`](../visual/reports/patch-4.8.0.2.md) —
a report is not a prompt.

**Not edited to match what gets built.** Where the built work deviates, the
deviation is recorded in [`../generation.md`](../generation.md) with a dated
note, per protocol 4.

---

## The patch brief, as issued

okay new patch to make sure everything goes back to something readable. whenever we swapped stats to be bars instead of number, revert that change. 

whenever we swapped to a pixelated font, we're going to have to choose a different px size or to abandon pixel fonts because the human view is still distorted (pixels dont' line up and 2 looks like a weird 8

give up button in learn move page isn't a button and does nothing.

events that say 'requires x' doesnt work. taking that event just brings a random event from v1, with no mention of the events conclusions that we want for a payoff for having the right species/HM

in the gym reward for move, there is no description for what the move is. give it some info (using explain, if possible to reuse that) so that player can choose for a physical or special based mon

first, report on current status of all these issues, then label this branch 4.8.0.2 patch, and describe the lift for how to accomplish this, then give me a report to give back to the project where I'm tracking all the features for this
