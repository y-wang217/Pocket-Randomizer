# Handoff: GYMRUN 4.11, the plan before Tier 0

**2026-09-25.** Written for a session picking this up cold. Read
[`../../CLAUDE.md`](../../CLAUDE.md) first, then the plan, then this.

## Where everything is

| | |
|---|---|
| The prompt and the plan | [`../spec/gymrun-stage4.11-weather-terrain-and-trigger-visuals.md`](../spec/gymrun-stage4.11-weather-terrain-and-trigger-visuals.md), filed verbatim before any work with the investigation and the tiers under it |
| The branch | `claude/dazzling-archimedes-wc1frw`, the 4.11 trunk. **No `src/` change on it yet**, by the plan's own hard stop |
| The three rows waiting on rulings | D47, D48, D49 in [`../design/bible-discrepancies.md`](../design/bible-discrepancies.md) |
| The register row | [`../spec/README.md`](../spec/README.md), status `draft` |
| The evidence the plan reads from | [`../reports/battle-anim-2-protocol-census.md`](../reports/battle-anim-2-protocol-census.md), 699 battles |

## What was found, in one paragraph

The weather logic is `@pkmn/sim`'s and has run in every fight since Stage 0.
The tree reads it as an event only: `core/battle/flags.ts` emits a `field` flag
when weather or terrain starts, the strip prints *Rain*, and `mark-sweep` runs
inside the causing actor's box. Nothing carries the state: `BattleFacts` has no
field, `battle.field.weather` is never read, and the world behind the battle
(`createWorldScene`, keyed by `data-locale`) knows nothing about the fight.
Every weather in this game is set by an ability, because `gen-pools.ts`
excludes the setter moves on purpose, and abilities fire on switch-in, which is
the one protocol batch `screens/battle.ts` shows with `animate=false`. So the
commonest weather start is invisible outside the log, and the setter never gets
an `ability` flag because the reader drops the `[from]` tag.

## What to do first

1. **Do not touch `src/`.** Tier 0 is a hard stop: run the sub-keyed census
   (`scripts/protocol-census.ts`, extended per the plan's 4.4 table), write
   `docs/reports/stage-4.11-field-census.md`, and wait for D47 to D49 to be
   ruled. The bible goes to Rev 14 on the rulings.
2. Confirm the sim's `Field` member names against the installed
   `@pkmn/sim` (`weather`, `terrain`, `weatherState.duration`,
   `effectiveWeather()`): `node_modules` was not present in the session that
   wrote the plan.
3. Then Tier 1, headless: `BattleFacts.field` and `data/fieldCopy.ts`, with
   `test/content-hash.test.ts` holding the exclusion.

## Rules this plan depends on

- The bible outranks the plan on every presentation question. A tier with an
  unruled row against it is not built.
- No version axis moves. Assert `contentHash` unmoved at every checkpoint.
- `scene.ts` is handed marks and never reads flags (`test/boundaries.test.ts`).
  The trigger pulse at Tier 4 keeps that shape.
- Weather tokens are global, never per locale (`test/visual-locales.test.ts`).
- Where the build deviates from the plan, the plan is not edited: the
  deviation goes in `docs/generation.md` §81 with a dated note.
