# Handoff: GYMRUN 4.10, Tier 6

**2026-09-23.** Written as Tier 6 closes, on `claude/dazzling-faraday-tafnyp`
([#68](https://github.com/y-wang217/Pocket-Randomizer/pull/68)). Read
[`../../CLAUDE.md`](../../CLAUDE.md), then [`4.10-tier-6-prep.md`](4.10-tier-6-prep.md),
then this.

## What shipped

| Item | Status | Account |
|---|---|---|
| M6.0 Starter move card (new item, D40) | done | [`../generation.md` §73](../generation.md) |
| M6.2 Coach marks re-anchored | done | §74 |
| M6.3 Pocket default | done | §75 |
| M6.1 Exposure labels | done | §76 |
| M6.4 Retire Simple and Detailed | **open**, gated on M7.1 | — |

Rulings D40 to D45, all ruled 2026-09-23. Bible Rev 12. No version axis moved;
`contentHash` is `0b2c2c`, as it was at the start of the tier.

## What Tier 7 inherits

**M7.1 is now the only thing M6.4 waits on.** The R6 row in section 9 decides
retirement, and section 9 records the decision as open. M6.4's two outcomes are
already defined: delete the Simple and Detailed branches, or ship the single
"numbers on stats" setting. D21's 13 drawer words were left for M6.4 because
they are the density picker's copy.

**The playtest protocol now has three things to observe that did not exist
before this tier:**

- **R7's count.** Section 9 bets that three exposures is enough and that the
  inspect rate on a family falls by run three. The labels ship at 1 and 3, the
  counter is in the settings store, and the family walk proves every family can
  fire. The observation needs an inspect count per family, which nothing records
  yet.
- **The classroom.** Starter select scrolls now (D45). Whether a first-run
  player reads past the first card is a direct observation.
- **The coach marks on the compact face.** They name press and hold where they
  used to name a tap. Section 9's R5 row is the one to watch.

**Open findings, none of them blocking:**

- The battle screen is 859px on a first run, 15 over the fold, because labels
  wrap on the move buttons. D44 records first run without gating it. If M7.1
  sees players miss the fourth move, that is the evidence.
- The result screen is 418px wide on `SMOKE24`, with or without labels, and it
  predates Tier 6. No test reads that surface's `scrollWidth`. An input to M7.2.
- The map drawer's glyphs are never labelled. The watcher counts the drawer or
  the routed screen, and the map overlay is neither. The map screen labels the
  same families, so no family goes unlabelled because of it.
- D38 (capture card fold) and D39 (the Toll's `Costs`) are still open.
- `heights.json`'s two stale blocks from Tier 5 are still stale; nothing in
  Tier 6 touched them.

## Instruments added

- `exposure=fresh|exhausted` on the gallery, defaulting to exhausted.
- The census's `first run` column and `starter card` row.
- `test/visual-exposure-labels.test.ts`: the family walk, and the classroom on
  both stores. `GYMRUN_RECORD=1` rewrites
  [`../visual/m6.1-starter-first-run.png`](../visual/m6.1-starter-first-run.png).
- The driven browsers' returning-player store (`scripts/first-launch.mjs`) puts
  every family past its third exposure. A family added to
  `src/data/glyphFamilies.ts` has to be added there too, and a unit test says so.
