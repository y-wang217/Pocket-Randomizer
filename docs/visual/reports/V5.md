# V5 report: battle stage composition, skipped

`V5 blocked: Release C not merged`

Stage V5 of `docs/spec/gymrun-visual-identity-plan.md` did not run. Its
overnight prompt names an extra precondition: the Release C protocol-to-flags
mapper and the HP chunk-and-shadow must exist and their tests pass, and if
Release C is not merged the stage prints the line above and exits zero with
no failed marker. A skip, not a failure.

## What was checked

- No file under `src/` or `test/` defines a flag mapper, a `flagWords`
  reader, an HP shadow, or names Release C. The only mention of Release C in
  the tree is a comment in `src/ui/theme/tokens.css` saying the motion token
  waits for its duration number.
- `docs/spec/README.md` lists the QoL release plan as `active` with no merge
  commit for any of its three releases, and `docs/README.md` carries
  Release A's R13 as an open blocker.
- `data/tuning.ts` carries no per-turn duration; `--motion-duration` is
  still 0.

## What V5 would need first

Release C, per `docs/spec/gymrun-qol-release-plan-rev2.md` section 6: R9
(HP falls as a chunk, shadow fades), R10 (first actor jiggles), R11 (flag
words off the protocol), R14 (berry-fired flag), and the one added-time
number in `data/tuning.ts`. The plan is explicit that V5 restyles those
rather than designs them, and that running V5 before Release C is the one
ordering that must not happen.

## What is already in place for it

- `stageChip` in `ui/chip.ts`, for the stat stages the panels will wear.
- The world scene the sprites would stand in (`createWorldScene`), with its
  near layer for the species swap to sink into and rise from.
- The battle screen's current measurements at 390x844 from the baseline:
  layout height 1083.5, scroll height 1232, four move buttons from 612 to
  840, above the fold by 4px. That is V5.1's "before" table, and the 600px
  target is the budget it has to reach.

No marker was written for V5. Re-running the loop after Release C merges
starts it from V5.1.
